// zest is a header-only library. This translation unit exists so that the
// `mcpp` build tool has a source to compile for the `lib` target; including the
// umbrella header validates that the public headers build cleanly.
#include <zest/zest.h>
