foreach(example IN ITEMS basic async custom_sink)
  add_executable(zest_${example} ${example}.cpp)
  target_link_libraries(zest_${example} PRIVATE zest::zest)
  zest_set_warnings(zest_${example})
endforeach()
